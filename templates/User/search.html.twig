{% set i = 0 %}
{% for user in users %}
    {% if user.Id != app.user.id %}
    <hr {% if i == 0 %}style="margin-top: 10vh;"{% endif %}>
<div id="ami" style="margin-bottom: 1%;">
    <a href="{{ path("joueur", {'id': user.Id }) }}"><img class="avatars" src="{{ asset(user.UserImage) }}" alt=""><p class="pseudojoueurs">{{  user.Username }}<br/>Score : {{ user.UserWin }}</p></a>
    <a class="defie" href="{{ path("creer2_partie", {'idadversaire': user.Id}) }}">Défier</a>
</div>
    {% set i = i+1 %}
        {% endif %}
{% endfor %}
{% if users == null %}
<p>Aucun résultat</p>
{% endif %}